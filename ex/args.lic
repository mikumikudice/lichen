// how to fetch CLI arguments in lichen

os mod = use "std/os.lim";

C efx;
ctext efx = mut cstr;

// FFI for printing C strings
fn puts(mut data cstr) u32 = C & ctext use "puts";

fn main() void = C & ctext & os {
    puts(os::args(1)? or ("none" cstr));
};
