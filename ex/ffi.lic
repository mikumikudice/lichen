// compile this with the -lc flag set,
// so it links against libc.

C efx;
ctext efx = mut cstr;

fn puts(mut fmt cstr) u32 = C & ctext use "puts";

pub fn main() void = C & ctext {
    puts("mornin' sailor!" cstr);
};
