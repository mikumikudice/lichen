// parser test for invalid record
// field access

type person = record {
    name str;
    age u8;
};

pub fn main() void = {
    let bob = person { name = "bob", age = 24 };
    let age = bob.id;
};
