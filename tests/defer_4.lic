// parser and compiler test for the
// `defer` statement semantics and
// implementation

io mod = use "std/io.lim";

pub fn main() void = io {
    let mut msg = "on scope";
    defer io::println(msg)!;
    defer msg = "at defer";
};
