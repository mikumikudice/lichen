// parsing test for global data parsing and FFI

msg str = "mornin' " + "sailor!\n";

stdout u32 = $rt_stdout;
fn puts(handle u32, data str) u32 = rt$rt_puts;

pub fn main() void = do {
    puts(stdout, msg);
};
