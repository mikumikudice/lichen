// parser test for asserting for effect
// tagging mismatch on function
// pointers

a efx = while;
b efx = mut str;

pub fn main() void = a {
    take_fun(main);
};

fn take_fun(fun fn() void a & b) unit = a {};
