// parser test for asserting for unset
// allocation lifetime

allocs efx = mut mem;

fn alloc(src str, mut a mem) str = allocs {
    return new ! src @ a;
};

pub fn main() void = allocs {
    new here | 20 {
        let foo = alloc("foo", here);
    }!;
};
