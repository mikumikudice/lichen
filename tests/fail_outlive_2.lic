// parser test to assert for fixed bug
// where for loops' local variable
// would outlive its scope

pub fn main() void = {
    for x = 4 u32; i .. [] u32 {
        unreachable;
    };
    let y = x;
};
