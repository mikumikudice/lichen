// parser test for invalid failure
// fallback on record field assignment

type foo = record {
    bar []u32;
};

type egg = record {
    fizz foo;
};

pub fn main() void = {
    let mut f = foo { bar = [8, 1, 2, 3] };
    let mut e = egg { fizz = f };
    e.fizz.bar[0]! or 0 = 0;
};
