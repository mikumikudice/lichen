// parser test for asserting against
// not-lifetimed variable assignment

pub fn main() void = {
    new global | 128 {
        let mut foo = "aaa";
        new local | 64 {
            let mut bar = new ! "bbb" @ local;
            foo = bar;
        }!;
    }!;
};
