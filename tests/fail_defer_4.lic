// parser test for assertinf against
// void expressions within defer

fn halt() void = {};

pub fn main() void = {
    defer if true {
        defer unreachable;
        if true {
            halt();
        };
    };
};
