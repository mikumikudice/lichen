// various tests for string operations

io mod = use "std/io.lim";

pub fn main() void = io {
    let text = "mornin' sailor!";
    let mut text' = "mornin' sailor!";

    if text == text' && #text == #text' {
        io::print("test ok\n")!;
    } else {
        io::print("test fail\n")!;
    };
};
