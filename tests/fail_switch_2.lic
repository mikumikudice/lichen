// parser test for switch type checking

io mod = use "std/io.lim";

pub fn main() void = io {
    let x = "foo";
    switch x;
    | "a".."c" do io::println("within a, b and c")!;
    | "bar" do io::println("bar")!;
    | else do io::println("something else")!;
};
