// parser test for asserting
// invalid mutable value passing of
// record field

fn take_ref(mut x []u8) unit = m { };

type foo = record {
    bar []u8;
    mut x u64;
};

pub fn main() void = m {
    let f = foo { bar = [4; 0...], x = 0 };
    take_ref(f.bar);
};
