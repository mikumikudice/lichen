// compiler test for testing memory
// arena creation failure and its
// fail state fallback statement
// execution

io mod = use "std/io.lim";

pub fn main() void = io {
    new arena | 0 {
        io::println("test fail")!;
    }! or io::println("test ok")!;
};
