// runtime test for correct asserts
// on fail states from a variables

fn foo(bar u32) !u32 = {
    if bar > 0 {
        return bar;
    } else {
        return error;
    };
};

pub fn main() void = {
    let res = foo(0);
    let res' = res!;
};
