// parser test for all mutable
// semantics and syntax

fn take_ref(mut x str) unit = m { };

pub fn main() void = m {
    let mut x = "foo";
    x = "bar";

    let y u32 = 4;
    let mut z = y;

    take_ref(x);
};
