// parser test to assert for fixed bug
// with field/value type mismatch

type person = record {
    name str;
    age u8;
};

pub fn main() void = io {
    let bob = person { name = "bob", name = 24 };
};
