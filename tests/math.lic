// parsing test for math operations and operators

io = use "io.lim";

pub fn main() void = io {
    let x u32 = 1 / 4;
    let y = (x - 1) i32;

    if (x == 0) && (y == -1) {
        io::println("test ok");
    } else {
        io::println("test fail");
    };
};
