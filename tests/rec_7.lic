// compiler test for record fields'
// alignment and offset emitting

type foo = record {
    a u8;
    b u16;
    c u8;
    d u64;
};

pub fn main() void = {
    let f = foo { a = 1, b = 2, c = 3, d = 4 };
    test f.a == 1 && f.b == 2 && f.c == 3 && f.d == 4;
};
