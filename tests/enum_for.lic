// parser test for interactions between
// the for-loop and enum types

type foo = enum u32 {
    a, b, c, d
};

fn main() void = {
    let mut sum = 0 u32;
    for v .. foo {
        sum = sum + v;
    };
    test sum == 6;
};
