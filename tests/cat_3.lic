// compiler test for checking if the
// concatenation machinery works with
// larger item types

pub fn main() void = {
    new here | 64 {
        let foo = [1, 2, 3] u64;
        let bar = [4, 5, 6] u64;
        let egg = cat foo ..! bar @ here;
        for itm, idx .. egg {
            test itm == idx + 1;
        };
    }!;
};
