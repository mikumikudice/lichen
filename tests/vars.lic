// parsing test for basic type checking
// and (global) declaration parsing.

w u32 = +1;

pub fn main() void = do {
    let x u32 = 6;
    let y u32 = x + w;

    let z  = (x * y) u64;
    let z' = 6700 + z;
};
