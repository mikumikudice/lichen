// parser test for asserting
// invalid mutable value passing of
// record field indexing

type foo = record {
    bar [8]u8;
    mut x u64;
};

pub fn main() void = {
    let f = foo { bar = [0...], x = 0 };
    let mut ref = f.bar[0..1];
};
