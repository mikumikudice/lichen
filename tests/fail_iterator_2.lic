// parser test for asserting against
// iterator reassignment within loop

pub fn main() void = {
    let mut foo = [1, 2, 3, 4] u32;
    let mut bar = foo;
    for i .. foo {
        for j .. bar {
            foo = bar;
        };
    };
};
