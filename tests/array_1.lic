// parser test for array literals
// type checking and emitting

pub fn main() void = {
    let arr   = [1, 2, 3, 4, 5] u32;
    let arr'  = ["hi", "hello"];
    let arr'' = [1.3, 9.7, 8.5, 1.21] f64;

    let spread [4]u32 = [0...];
    let spread' = [4; 1, 0...] u32;
};
